<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="crm_lead_view_form_inherit_crm_service" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.crm.service</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_complaints" type="object" class="oe_stat_button" icon="fa-ticket">
                    <field name="complaint_count" widget="statinfo" string="Khiếu nại"/>
                </button>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Chỉ số CRM">
                    <group>
                        <group string="Chỉ số bán hàng">
                            <field name="fail_reason"/>
                            <field name="is_repurchase"/>
                            <field name="satisfaction_score"/>
                        </group>
                        <group string="Chỉ số chăm sóc khách hàng">
                            <field name="first_response_date"/>
                            <field name="response_time_hours"/>
                            <field name="complaint_count"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
